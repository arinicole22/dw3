const Producto = ({subtitulo, descripcion, imagen}) => {

    return (
        <div>
            <h1>{productito}</h1>
            <p>{productito de clase}</p>
            <img src={urlimagencita} alt={imagencita}/>
        </div>
    )

}

export default Producto
